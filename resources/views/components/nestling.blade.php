<div class="nestling">
    <p class="title"><a href="{{route('nestlings.index', [$nest, $nestling->title])}}">{{$nestling->title}}</a></p>
    <p class="description">{{$nestling->description}}</p>
</div>